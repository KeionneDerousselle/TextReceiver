<UserControl x:Class="TextReceiver.Conversation.ConversationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conversation="clr-namespace:TextReceiver.Conversation"
             xmlns:message="clr-namespace:TextReceiver.Message"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="450"
             DataContext = "{Binding Source={StaticResource VMLocator}, Path=ConversationViewModel }"
             >
    <!--<UserControl.DataContext>
        <conversation:ConversationViewModel></conversation:ConversationViewModel>
    </UserControl.DataContext>-->
    <Grid>
        <StackPanel >
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
            <message:MessageView></message:MessageView>
        </StackPanel>
    </Grid>
</UserControl>
