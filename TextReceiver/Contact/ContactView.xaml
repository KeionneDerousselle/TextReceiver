<UserControl x:Class="TextReceiver.Contact.ContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TextReceiver.Views"
             xmlns:components="clr-namespace:TextReceiver.Views.common"
             DataContext = "{Binding Source={StaticResource VMLocator}, Path=ContactViewModel }"
             mc:Ignorable="d">
    <Button Background="Transparent" Command="{Binding ContactClickedCommand}">
        <Button.Template>
            <ControlTemplate TargetType="{x:Type Button}">
                <StackPanel Orientation="Horizontal" Background="Black">
                    <components:Avatar></components:Avatar>
                    <Label Foreground="White">Keionne Derousselle</Label>
                    <Ellipse Fill="LightGreen" Width="20" Height="20"></Ellipse>
                </StackPanel>
            </ControlTemplate>
        </Button.Template>
    </Button>

</UserControl>
